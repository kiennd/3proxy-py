# 3proxy Configuration Environment Variables

# Proxy Authentication Settings
# Set to 'true' to enable authentication, 'false' to disable
ENABLE_AUTH=true

# Proxy Username (only used when ENABLE_AUTH=true)
PROXY_USER=PROXY_USER

# Proxy Password (only used when ENABLE_AUTH=true)
PROXY_PASS=PROXY_PASS

# Flask Web Server Settings
# Host to bind the Flask server to (default: 0.0.0.0)
FLASK_HOST=0.0.0.0

# Port for the Flask web server (default: 5555)
FLASK_PORT=5555

# API Security
# API key for protected endpoints (default: 2222)
API_KEY=2222

# Network Interface Settings (optional overrides)
# NET_INTERFACE=eth0
# BASE_IPv4=103.77.241.181
# IPV6_PREFIX=2401:3660:0:c10c:

# 3proxy Paths (optional overrides)
# PROXY_BINARY_PATH=/root/3proxy/bin/3proxy
# CONFIG_DIR=/root/3proxy/bin

# Logging
# Enable debug logging (default: false)
# DEBUG=false

# Example usage:
# cp env.example .env
# Edit .env with your values
# set -a && source .env && set +a
# python3 proxy.py 